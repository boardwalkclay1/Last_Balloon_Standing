import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rollnconnect.balloon',
  appName: 'Balloon Dating Game',
  webDir: 'public',
  bundledWebRuntime: false,
};

export default config;
